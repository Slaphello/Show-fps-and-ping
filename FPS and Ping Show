local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("UserInputService")
local d = game:GetService("Stats")
local e = game:GetService("TweenService")
local f = a.LocalPlayer
if not f then
    warn("LocalPlayer not found bruh")
    return
end
local function g()
    local h = f:WaitForChild("PlayerGui")
    if gethui then
        h = gethui()
    elseif get_hidden_gui then
        h = get_hidden_gui()
    end
    return h
end
local i = g()
if not d or not d.Network or not d.Network.ServerStatsItem or not d.Network.ServerStatsItem:FindFirstChild("Data Ping") then
    warn("Stats service not found bruh")
    return
end
local j = Instance.new("ScreenGui")
j.Name = "PingAndFPS_Display"
j.ResetOnSpawn = false
j.IgnoreGuiInset = true
j.Parent = i
local k = Instance.new("Frame")
k.Name = "MainFrame"
k.Size = UDim2.new(0, 160, 0, 32)
k.Position = UDim2.new(0, -200, 0, 10)
k.BackgroundTransparency = 1
k.BackgroundColor3 = Color3.new(0, 0, 0)
k.Parent = j
local l = Instance.new("UICorner")
l.CornerRadius = UDim.new(0, 12)
l.Parent = k
local m = Instance.new("TextLabel")
m.Name = "StatsLabel"
m.Size = UDim2.new(1, -8, 1, -6)
m.Position = UDim2.new(0, 4, 0, 3)
m.TextColor3 = Color3.new(1, 1, 1)
m.TextScaled = true
m.Font = Enum.Font.SourceSansBold
m.BackgroundTransparency = 1
m.RichText = true
m.TextTransparency = 1
m.Text = "Ping: -- ms | FPS: --"
m.Parent = k
local function n()
    i:SetAttribute("LastUIPosX", k.Position.X.Offset)
    i:SetAttribute("LastUIPosY", k.Position.Y.Offset)
end
local function o()
    local p = i:GetAttribute("LastUIPosX") or 10
    local q = i:GetAttribute("LastUIPosY") or 10
    local r = workspace.CurrentCamera.ViewportSize
    p = math.clamp(p, 0, r.X - k.Size.X.Offset)
    q = math.clamp(q, 0, r.Y - k.Size.Y.Offset)
    return UDim2.new(0, p, 0, q)
end
k.Position = o()
local function s(t)
    local u = table.clone(t)
    table.sort(u)
    local v = #u
    if v % 2 == 0 then
        return (u[v / 2] + u[v / 2 + 1]) / 2
    else
        return u[math.ceil(v / 2)]
    end
end
local w = 1
local x = tick()
local y = 0
local z = 60
local A = 0
local B = 0.2
local C = 0.05
local D = {}
local E = 10
local F = 50
local function G(H)
    if H >= 60 then
        return Color3.fromRGB(0, 255, 0)
    elseif H >= 30 then
        return Color3.fromRGB(255, 255, 0)
    else
        return Color3.fromRGB(255, 0, 0)
    end
end
local function I(H)
    if H < 80 then
        return Color3.fromRGB(0, 255, 0)
    elseif H < 150 then
        return Color3.fromRGB(255, 255, 0)
    else
        return Color3.fromRGB(255, 0, 0)
    end
end
local function J()
    local K = I(F)
    local L = G(z)
    m.Text =
        string.format(
        '<font color="rgb(255,255,255)">Ping:</font> <font color="rgb(%d,%d,%d)">%d ms</font> | <font color="rgb(255,255,255)">FPS:</font> <font color="rgb(%d,%d,%d)">%d</font>',
        K.R * 255,
        K.G * 255,
        K.B * 255,
        F,
        L.R * 255,
        L.G * 255,
        L.B * 255,
        z
    )
end
b.Heartbeat:Connect(
    function(M)
        y = y + 1
        local N = math.clamp(d.Network.ServerStatsItem["Data Ping"]:GetValue(), 0, 999)
        table.insert(D, N)
        if #D > E then
            table.remove(D, 1)
        end
        local O = tick()
        if O - x >= w then
            local P = y / (O - x)
            local Q = z
            z = B * P + (1 - B) * (z + A)
            A = C * (z - Q)
            if #D > 0 then
                F = s(D)
            end
            J()
            y = 0
            x = O
        end
    end
)
local R = false
local S, T
k.InputBegan:Connect(
    function(U)
        if U.UserInputType == Enum.UserInputType.MouseButton1 or U.UserInputType == Enum.UserInputType.Touch then
            R = true
            S = U.Position
            T = k.Position
            U.Changed:Connect(
                function()
                    if U.UserInputState == Enum.UserInputState.End then
                        R = false
                        n()
                    end
                end
            )
        end
    end
)
c.InputChanged:Connect(
    function(U)
        if R and (U.UserInputType == Enum.UserInputType.MouseMovement or U.UserInputType == Enum.UserInputType.Touch) then
            local V = U.Position - S
            local W = math.clamp(T.X.Offset + V.X, 0, workspace.CurrentCamera.ViewportSize.X - k.Size.X.Offset)
            local X = math.clamp(T.Y.Offset + V.Y, 0, workspace.CurrentCamera.ViewportSize.Y - k.Size.Y.Offset)
            k.Position = UDim2.new(0, W, 0, X)
        end
    end
)
local Y = 0
local Z = 0.5
local _ = Instance.new("TextButton")
_.Name = "ToggleStatsButtLOL"
_.Text = "Toggle Stats"
_.Size = UDim2.new(0, 100, 0, 25)
_.Position = UDim2.new(0, 10, 0, 70)
_.BackgroundColor3 = Color3.new(0, 0, 0)
_.BackgroundTransparency = 0.7
_.TextColor3 = Color3.new(1, 1, 1)
_.TextScaled = true
_.Parent = j
_.MouseButton1Click:Connect(
    function()
        local O = tick()
        if O - Y < Z then
            return
        end
        Y = O
        local a0 = workspace.CurrentCamera.ViewportSize
        if k.Visible then
            local a1 = k.Position.X.Offset < a0.X / 2 and -k.Size.X.Offset or a0.X
            local a2 = UDim2.new(0, a1, 0, k.Position.Y.Offset)
            local a3 =
                e:Create(k, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = a2})
            a3:Play()
            a3.Completed:Connect(
                function()
                    k.Visible = false
                end
            )
        else
            local a4 = o()
            local a1 = a4.X.Offset < a0.X / 2 and -k.Size.X.Offset or a0.X
            k.Position = UDim2.new(0, a1, 0, a4.Y.Offset)
            k.Visible = true
            local a5 =
                e:Create(k, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = a4})
            a5:Play()
        end
    end
)
local a6 =
    e:Create(k, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = 0.5})
local a7 = e:Create(m, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextTransparency = 0})
task.wait(0.5)
a6:Play()
a7:Play()
        local rawPing = math.clamp(Stats.Network.ServerStatsItem["Data Ping"]:GetValue(), 0, 999)
        table.insert(pingBuffer, rawPing)
        if #pingBuffer > pingLimit then
            table.remove(pingBuffer, 1)
        end

        local now = tick()
        if now - lastUpdate >= updateInterval then
            local rawFPS = frameCount / (now - lastUpdate)
            local prevFPS = fpsEstimate
            fpsEstimate = alpha * rawFPS + (1 - alpha) * (fpsEstimate + fpsTrend)
            fpsTrend = beta * (fpsEstimate - prevFPS)
            if #pingBuffer > 0 then
                pingEstimate = median(pingBuffer)
            end
            updateUI()
            frameCount = 0
            lastUpdate = now
        end
    end
)

local dragging = false
local dragStart, startPos

mainFrame.InputBegan:Connect(
    function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = mainFrame.Position
            input.Changed:Connect(
                function()
                    if input.UserInputState == Enum.UserInputState.End then
                        dragging = false
                        saveUIPosition()
                    end
                end
            )
        end
    end
)

UserInputService.InputChanged:Connect(
    function(input)
        if
            dragging and
                (input.UserInputType == Enum.UserInputType.MouseMovement or
                    input.UserInputType == Enum.UserInputType.Touch)
         then
            local delta = input.Position - dragStart
            local newX =
                math.clamp(
                startPos.X.Offset + delta.X,
                0,
                workspace.CurrentCamera.ViewportSize.X - mainFrame.Size.X.Offset
            )
            local newY =
                math.clamp(
                startPos.Y.Offset + delta.Y,
                0,
                workspace.CurrentCamera.ViewportSize.Y - mainFrame.Size.Y.Offset
            )
            mainFrame.Position = UDim2.new(0, newX, 0, newY)
        end
    end
)

local lastToggle = 0
local toggleDebounce = 0.5

local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleStatsButton"
toggleButton.Text = "Toggle Stats"
toggleButton.Size = UDim2.new(0, 100, 0, 25)
toggleButton.Position = UDim2.new(0, 10, 0, 70)
toggleButton.BackgroundColor3 = Color3.new(0, 0, 0)
toggleButton.BackgroundTransparency = 0.7
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.TextScaled = true
toggleButton.Parent = screenGui

toggleButton.MouseButton1Click:Connect(
    function()
        local now = tick()
        if now - lastToggle < toggleDebounce then
            return
        end
        lastToggle = now

        local viewSize = workspace.CurrentCamera.ViewportSize
        if mainFrame.Visible then
            local offX = mainFrame.Position.X.Offset < viewSize.X / 2 and -mainFrame.Size.X.Offset or viewSize.X
            local target = UDim2.new(0, offX, 0, mainFrame.Position.Y.Offset)
            local tweenOut =
                TweenService:Create(
                mainFrame,
                TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out),
                {Position = target}
            )
            tweenOut:Play()
            tweenOut.Completed:Connect(
                function()
                    mainFrame.Visible = false
                end
            )
        else
            local start = loadUIPosition()
            local offX = start.X.Offset < viewSize.X / 2 and -mainFrame.Size.X.Offset or viewSize.X
            mainFrame.Position = UDim2.new(0, offX, 0, start.Y.Offset)
            mainFrame.Visible = true
            local tweenIn =
                TweenService:Create(
                mainFrame,
                TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out),
                {Position = start}
            )
            tweenIn:Play()
        end
    end
)

local fadeTween =
    TweenService:Create(
    mainFrame,
    TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
    {BackgroundTransparency = 0.5}
)
local labelTween =
    TweenService:Create(
    statsLabel,
    TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
    {TextTransparency = 0}
)
task.wait(0.5)
fadeTween:Play()
labelTween:Play()
